---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import Card from '../../components/Card.astro';
import { Calendar, User, ExternalLink } from 'lucide-vue-next';

const writeups = await getCollection('writeups');
const sortedWriteups = writeups.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const categories = ['All', 'Web', 'Pwn', 'Crypto', 'OSINT', 'Reverse Engineering', 'Forensics'];
---

<Layout title="Writeups">
	<div class="max-w-7xl mx-auto px-4 py-12 md:py-20">
		<header class="mb-12 md:mb-16 px-2">
			<h1 class="text-3xl md:text-4xl font-black tracking-tighter mb-4">Technical Archive</h1>
			<p class="text-zinc-500 text-sm md:text-base">Deep dives into vulnerabilities, exploits, and CTF challenges.</p>
		</header>

		<!-- Categories -->
		<div class="flex flex-wrap gap-2 mb-10 md:mb-12 px-2">
			{categories.map((cat) => (
				<button class="px-4 py-2 rounded-full border border-stealth text-[11px] md:text-xs font-bold hover:border-zinc-700 hover:bg-white/5 transition-all active:scale-95 tap-target">
					{cat}
				</button>
			))}
		</div>

		<!-- Grid -->
		<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 md:gap-6 px-2">
			{sortedWriteups.map((post) => {
				const isExternal = !!post.data.externalUrl;
				const href = isExternal ? post.data.externalUrl : `/writeups/${post.id}`;
				const target = isExternal ? "_blank" : "_self";
				
				return (
					<a href={href} target={target} rel={isExternal ? "noopener noreferrer" : ""} class="block group">
						<Card glow class="h-full flex flex-col p-0 overflow-hidden group-hover:border-accent-purple/30 transition-colors duration-300">
							{post.data.image && (
								<div class="relative w-full aspect-video overflow-hidden border-b border-stealth">
									<img 
										src={post.data.image} 
										alt={post.data.title} 
										class="object-cover w-full h-full filter grayscale group-hover:grayscale-0 transition-all duration-700 group-hover:scale-105"
									/>
									<div class="absolute inset-0 bg-gradient-to-t from-background/90 via-background/20 to-transparent pointer-events-none"></div>
								</div>
							)}
							<div class="p-6 md:p-5 flex flex-col flex-grow">
								<div class="flex justify-between items-start mb-4">
									<span class="px-2.5 py-1 rounded-md bg-accent-purple/10 text-accent-purple text-[10px] font-bold uppercase tracking-widest border border-accent-purple/20">
										{post.data.category}
									</span>
									{isExternal && <ExternalLink class="w-4 h-4 text-zinc-600 group-hover:text-accent-cyan transition-colors" />}
								</div>
								<h3 class="text-xl font-bold mb-3 group-hover:text-white transition-colors leading-tight">
									{post.data.title}
								</h3>
								<p class="text-sm text-zinc-500 line-clamp-3 md:line-clamp-2 mb-8 flex-grow leading-relaxed">
									{post.data.description}
								</p>
								<div class="flex items-center justify-between text-[10px] text-zinc-600 font-mono uppercase tracking-[0.2em] pt-4 border-t border-stealth">
									<div class="flex items-center gap-1.5">
										<Calendar class="w-3.5 h-3.5" />
										{post.data.pubDate.toLocaleDateString()}
									</div>
									<div class="flex items-center gap-1.5">
										<User class="w-3.5 h-3.5" />
										{post.data.author}
									</div>
								</div>
							</div>
						</Card>
					</a>
				);
			})}
		</div>
	</div>
</Layout>
